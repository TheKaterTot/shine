html
  head
    link(rel="stylesheet", href="/rmodal-no-bootstrap.css")
    link(rel="stylesheet", href="/animate.css")
    link(rel="stylesheet", href="/application.css")
    title Shine
  body
    header
      span.user-session
        - if(user)
          span Hi, #{user.username}
          a(href="/logout") Logout
        - else
          a(href="/login") Sign in via Twitter
      img(src="images/shine_logo.png")
      div.intro
        p You feel a little gray today. That's okay. Use the arrow keys to take a walk. Maybe you'll find something to shine about.
    #game-screen
    #modal.modal
      .modal-dialog.animated
        .modal-content
          .modal-header
          .modal-body
          .modal-footer
            button.btn Shine On
    script(src="/app.bundle.js")
    
      
